name: Visual Regression Testing
on:
  push:
    branches: ["main"]
  schedule:
    # 毎日 9:00 (JST)
    - cron: "0 0 * * *"

jobs:
  e2e:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: docker compose build
        run: docker compose -f docker-compose.vrt.yml build

      - name: Run Playwright tests
        run: docker compose -f ./web/docker-compose.vrt.yml up --abort-on-container-exit --exit-code-from test

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: web/playwright-report/
          retention-days: 30
